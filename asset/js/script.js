// Example JavaScript for additional interactivity
document.querySelectorAll('.button').forEach(button => {
    button.addEventListener('click', () => {
        alert('Direct you nowww!');
    });
});
